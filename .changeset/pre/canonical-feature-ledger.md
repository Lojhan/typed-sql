---
"@typed-sql/conformance": minor
"@typed-sql/mysql": patch
---

Add the canonical, version-aware grammar feature ledger API and fail closed for PostgreSQL-style `UPDATE FROM` and `INSERT DEFAULT VALUES` when analyzed by the MySQL grammar.

---
"@typed-sql/mysql": minor
---

Complete MySQL data-modification parsing and analysis for insert modifiers, inserted-row aliases,
duplicate-key updates, `REPLACE`, joined multi-table updates and deletes, partitions, and
single-table ordering and limits.

/** Extension manifests are outside the focused homepage playground schema subset. */
export function postgresExtensionCasts(): readonly never[] {
  return [];
}

/** Extension manifests are outside the focused homepage playground schema subset. */
export function postgresExtensionOperators(): readonly never[] {
  return [];
}
